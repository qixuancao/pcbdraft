services:
  pcbdraft:
    build:
      context: .
    image: ghcr.io/qixuancao/pcbdraft:latest
    ports:
      - "127.0.0.1:8765:8765"
    volumes:
      - pcbdraft-data:/data
    restart: unless-stopped

volumes:
  pcbdraft-data:
