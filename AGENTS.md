# PCBDraft Agent 工作约定

## 主 Agent 审查与子 Agent 执行分工

- 主 Agent 负责总体审查、架构与关键判断、任务拆解、协调以及最终验收。
- 具体编码、重构、文档或配置修改及定向验证，默认委派给子 Agent 执行。
- 派发任务时明确范围、文件归属、预期产出和验证要求，避免覆盖用户或其他任务的工作。
- 主 Agent 必须审查子 Agent 的改动和验证证据，确认满足任务目标后再交付。

## 每轮修改及时提交并推送 Git

- 每完成一轮代码、文档或配置修改，并完成本轮对应的最小快速验证后，立即精确暂存和提交本轮相关文件，作为可回溯的迭代检查点；随后将该提交推送到当前工作分支，首次推送时设置该分支的 upstream。
- 每个提交只包含本轮任务的相关改动，保留用户或其他任务尚未提交的工作；提交说明应概述本轮变更及验证结果。
- 默认禁止强制推送、合并 `main`、删除分支、修改 remote，或将无关脏文件纳入提交；这些操作只有在用户另行明确授权其精确范围后才能执行。
- 推送失败时如实报告本地提交状态和原始错误，不得将仅存在于本地的提交表述为远端成功。推送成功后，回读远端当前分支的精确 ref，并核对其 SHA 与本地提交一致后再报告成功。
- 大型重构按可验证的阶段拆分提交。提交和推送都不得改变下述快速验证预算，也不自动触发全量测试或发布门禁。

## 默认采用快速迭代验证

- 普通开发、修复和本地 Git 提交是迭代检查点，不是发布门禁。不要仅因为用户要求“提交”就运行全量测试。
- 提交前默认只运行与本次改动直接相关的最小验证：`git diff --check`、改动文件的 lint/format，以及最接近改动行为的单元测试模块或用例。
- 快速验证默认总预算约 90 秒。预计超过预算的测试应缩小范围；已经明显超时且没有新决策价值时应停止，并如实报告未完成项。
- 除非用户明确要求，或正在准备合并、发布、发布候选，不要运行 `scripts/test.sh`、全量 `unittest discover`/coverage、完整 KiCad 验收、TUI/Browser E2E、Python 版本矩阵、依赖审计或 `scripts/release-check.sh`。
- 只有当改动直接触及某个高风险子系统时，才补充该子系统最小的针对性集成测试；不要把“可能更安心”本身当作扩大测试范围的理由。
- 允许多个主题分支或 worktree 并行快速推进。全量验证可以集中到专门的集成分支、合并门禁或 CI 中执行，不要求每个分支的每次提交重复承担同一套长测试。
- 快速检查发现确定性失败时，修复或明确报告后再提交。若未跑全量测试，提交不因此被阻塞，但交付时必须清楚列出已运行、被中止和留待 CI/集成阶段的验证。
